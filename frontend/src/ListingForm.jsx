import React, {useState} from 'react'
import {useNavigate} from 'react-router-dom'

const ListingForm = () => {
    // UI
    return(
        <div>
            <button>
                Add An Item
            </button>
        </div>
    )
}

export default ListingForm